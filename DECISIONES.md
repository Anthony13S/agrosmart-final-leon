# 🧭 DECISIONES.md — Bitácora de diseño

> **Instrucciones.** Completa **una entrada por fase**, en **primera persona** y
> **refiriéndote a tu propio código**: nombres reales de tus clases, tu tabla, tus
> líneas, tu salida real de terminal.
>
> ❌ **No puntúa** una justificación genérica que podría pegarse en cualquier proyecto
> (ej.: *"usé boundedElastic porque es una buena práctica para operaciones bloqueantes"*).
> ✅ **Sí puntúa** una justificación anclada a tu código (ej.: *"en `ProductoService`
> línea 34 envolví `productoRepository.findAll()` porque Hibernate abre la conexión
> JDBC en el hilo llamante; al probarlo sin `subscribeOn` vi en el log el hilo
> `reactor-http-nio-2`, que es el event loop de Netty"*).
>
> Estas mismas preguntas se te harán en la **defensa oral**.

---

## Datos

- **Nombre:** Anthony Samir Leon Jativa
- **Cedula:** 1728626993
- **NN (dos ultimos digitos):** 93
- **Categoria asignada (segun el ultimo digito):** Cafe (ultimo digito 3)

---

## Fase 1 — Configuracion y perfiles

**1.1** Que archivo activa el perfil `prod` y que linea exacta lo hace?

En `src/main/resources/application.properties` la linea `spring.profiles.active=prod`
es la que activa el perfil Con eso Spring Boot busca automaticamente el archivo
`application-prod.properties` y carga esa configuracion por encima de la base

**1.2** Pega la linea del log de arranque donde se ve tu puerto y el perfil activo

```
The following 1 profile is active: "prod"
o.s.boot.reactor.netty.NettyWebServer    : Netty started on port 8193 (http)
```

**1.3** Que habria pasado si dejabas `ddl-auto=create-drop` en lugar de `update`?
Responde pensando en tus datos sembrados

Con `create-drop`, Hibernate borra la tabla `tbl_productos_base_93` al apagar la
aplicacion y la vuelve a crear vacia en el siguiente arranque Mi `CommandLineRunner`
volveria a sembrar los 5 productos cada vez, pero perderia cualquier dato que hubiera
insertado manualmente entre arranques (por ejemplo, si hubiera probado insertar un
producto nuevo desde psql para hacer pruebas, se perderia) Con `update`, la tabla y
los datos persisten entre reinicios, y solo se aplican cambios de esquema si cambio la
entidad

**1.4** Levantaste PostgreSQL con `compose.yaml` (Opcion A) o con una instalacion local
(Opcion B)? Que ventaja tiene la que elegiste?

Empece intentando la Opcion A con Docker Compose Support, pero Docker Desktop no pudo
iniciar en mi maquina porque no detecto soporte de virtualizacion habilitado Cambie a
la Opcion B: instale PostgreSQL localmente y declare la conexion manualmente en
`application-prod.properties` (`spring.datasource.url`, `username`, `password`
apuntando a `localhost:5432/agrosmart_db`) La ventaja de la Opcion B en mi caso fue que
no dependia de resolver el problema de virtualizacion de mi equipo para poder avanzar,
y la conexion es estable una vez que el servicio de PostgreSQL esta corriendo

---

## Fase 2 — Persistencia con JPA/Hibernate

**2.1** Cual es el nombre exacto de tu tabla y de donde salio ese nombre?

Mi tabla se llama `tbl_productos_base_93` Sale de mi semilla personal: los dos ultimos
digitos de mi cedula son 93, y el enunciado indica que el nombre de la tabla es
`tbl_productos_base_` seguido de esos dos digitos

**2.2** Pega la salida de `psql -d agrosmart_db -c "\d tbl_productos_base_NN"` y
senala donde se ve la restriccion `unique` y el `length` de 120

```
                                    Tabla "public.tbl_productos_base_93"
        Columna         |          Tipo          | Ordenamiento | Nulable |              Por omision
-------------------------+------------------------+--------------+---------+---------------------------------------
 id_producto             | bigint                 |              | not null| generated by default as identity
 categoria               | character varying(40)  |              |         |
 correos_notificacion    | character varying(500) |              |         |
 nombre_producto         | character varying(120) |              | not null|
 precio_usd              | numeric(10,2)          |              |         |
 stock_kg                | integer                |              | not null|
Indices:
    "tbl_productos_base_93_pkey" PRIMARY KEY, btree (id_producto)
    "ukm0xcqivhqldgyf12u1y003dji" UNIQUE CONSTRAINT, btree (nombre_producto)
```

El `length` de 120 se ve en la columna `nombre_producto`, tipo `character varying(120)`
La restriccion `unique` se ve en el indice `ukm0xcqivhqldgyf12u1y003dji`, un `UNIQUE
CONSTRAINT` sobre `nombre_producto` — Hibernate lo genero automaticamente a partir de
`@Column(unique = true)` en mi entidad

**2.3** Por que usaste `BigDecimal` y no `double` para `precio_usd`? Relacionalo con el
tipo que genero Hibernate en PostgreSQL

`double` usa representacion binaria de punto flotante, que puede introducir errores de
redondeo en calculos con dinero (por ejemplo, 0.1 + 0.2 no da exactamente 0.3)
`BigDecimal` representa el numero de forma exacta en base 10, lo cual es correcto para
manejar precios Esto se refleja en como Hibernate mapeo la columna: la genero como
`numeric(10,2)` en PostgreSQL, un tipo decimal exacto con 10 digitos de precision y 2
de escala, coherente con `@Column(precision = 10, scale = 2)` en mi entidad

**2.4** Como hiciste idempotente tu siembra y que pasaria en el segundo arranque si no
lo fuera? (piensa en la restriccion `unique` de `nombre_producto`)

En mi `CommandLineRunner` (dentro de `AgroSmartApplication.java`) envolvi la siembra en
`if (repository.count() == 0) { ... }`, asi que solo inserta los 5 productos si la
tabla esta vacia Si no lo fuera, cada arranque intentaria volver a insertar los mismos
5 productos con los mismos nombres, y como `nombre_producto` tiene la restriccion
`unique`, el segundo arranque lanzaria una excepcion de violacion de restriccion unica
(`duplicate key value violates unique constraint`) y probablemente la aplicacion
fallaria al iniciar

---

## Fase 3 — Modelo inmutable y logica funcional

**3.1** Por que tienes **dos** clases (`ProductoEntity` y `Producto`) en lugar de una?
Que te impide hacer inmutable directamente la entidad de Hibernate?

Hibernate necesita instanciar objetos por reflexion y llenarlos campo por campo cuando
lee filas de la base de datos, por eso exige un constructor vacio y setters en
`ProductoEntity` — no puede trabajar con una clase `final` sin setters Si intentara
hacer inmutable directamente la entidad, Hibernate no podria materializar los objetos
al consultar la base Por eso separe las dos: `ProductoEntity` es la clase mutable que
el ORM necesita, y `Producto` es mi modelo de dominio 100% inmutable, sin relacion con
Hibernate `ProductoMapper.toDominio()` es el puente entre ambas

**3.2** Escribe el codigo exacto de **tus dos** copias defensivas e indica en que linea
esta cada una

```java
// En el constructor de Producto.java (copia defensiva de ENTRADA)
public Producto(Long id, String nombre, String categoria, BigDecimal precioUsd,
                 List<String> correosNotificacion) {
    this.id = id;
    this.nombre = nombre;
    this.categoria = categoria;
    this.precioUsd = precioUsd;
    this.correosNotificacion = new ArrayList<>(correosNotificacion);
}

// En el getter de Producto.java (copia defensiva de SALIDA)
public List<String> getCorreosNotificacion() {
    return Collections.unmodifiableList(new ArrayList<>(correosNotificacion));
}
```

**3.3** Por que la copia defensiva **solo en el getter** no seria suficiente? Describe
el ataque concreto que quedaria abierto sobre **tu** clase

Si en el constructor hiciera `this.correosNotificacion = correosNotificacion;` (sin
copiar), mi objeto `Producto` guardaria la misma referencia de lista que me paso quien
lo construyo Ese codigo externo podria seguir teniendo esa misma lista en una
variable, y hacer `listaOriginal.add("intruso@mail.com")` despues de crear el
`Producto` — como es la misma referencia, mi objeto "inmutable" cambiaria su contenido
interno sin que yo lo autorizara, aunque el getter devuelva una copia protegida hacia
afuera La copia del getter protege la salida, pero no evita que alguien manipule la
entrada por la puerta trasera del constructor

**3.4** Como implementaste `A_MAYUSCULAS` para no mutar el `Producto` recibido?

```java
public static final Function<Producto, Producto> A_MAYUSCULAS = producto ->
        new Producto(
                producto.getId(),
                producto.getNombre().toUpperCase(),
                producto.getCategoria(),
                producto.getPrecioUsd(),
                new ArrayList<>(producto.getCorreosNotificacion())
        );
```

Construyo una instancia completamente nueva de `Producto`, copiando todos los campos
del original y solo cambiando el nombre a mayusculas El producto que recibi como
parametro nunca se toca ni se modifica

---

## Fase 4 — Servicio reactivo y aislamiento del bloqueo

**4.1** Pega tu metodo `obtenerProductosComercializables()` completo

```java
public Flux<Producto> obtenerProductosComercializables() {
    return Mono.fromCallable(repository::findAll)
            .subscribeOn(Schedulers.boundedElastic())
            .flatMapMany(Flux::fromIterable)
            .map(ProductoMapper::toDominio)
            .map(ProductoFilters.A_MAYUSCULAS)
            .filter(ProductoFilters.IS_VALID)
            .doOnNext(ProductoFilters.LOG_PRODUCTO)
            .defaultIfEmpty(PRODUCTO_GENERICO);
}
```

**4.2** Que pasa **exactamente** si eliminas
`.subscribeOn(Schedulers.boundedElastic())` de ese metodo? Si lo probaste, indica que
hilo aparecia en el log antes y despues

Sin `subscribeOn(boundedElastic)`, la llamada bloqueante `repository.findAll()` se
ejecutaria en el mismo hilo que recibio la peticion HTTP, que en WebFlux es un hilo del
event loop de Netty (del tipo `reactor-http-nio-N`) Ese hilo quedaria bloqueado
esperando la respuesta de PostgreSQL, y como el event loop atiende todas las peticiones
concurrentes con un numero reducido de hilos, esa unica peticion podria degradar el
tiempo de respuesta de todas las demas peticiones simultaneas de la aplicacion Con
`subscribeOn(boundedElastic)`, la llamada se ejecuta en un hilo del pool
`boundedElastic` (del tipo `boundedElastic-N`), separado del event loop

**4.3** Por que `Mono.fromCallable(...)` y no `Mono.just(repository.findAll())`?
(pista: cuando se ejecuta cada uno)

`Mono.just(repository.findAll())` ejecutaria `repository.findAll()` inmediatamente, en
el momento en que se construye el `Mono`, sin importar si alguien se suscribe despues o
no — es decir, la consulta bloqueante se dispararia en el hilo que esta armando el
flujo reactivo, antes de que `subscribeOn` tenga oportunidad de moverla a otro hilo
`Mono.fromCallable(...)` en cambio difiere la ejecucion: el `Callable` no se invoca
hasta que alguien se suscribe al `Mono`, y ahi si `subscribeOn` puede decidir en que
hilo se ejecuta esa llamada diferida

**4.4** En **tu** codigo, donde usaste `defaultIfEmpty` y donde `switchIfEmpty`, y por
que no son intercambiables en esos dos lugares?

Use `defaultIfEmpty(PRODUCTO_GENERICO)` en `obtenerProductosComercializables()`: si tras
filtrar no queda ningun producto comercializable, el `Flux` sigue completandose
normalmente (sin error), pero emite un unico producto de respaldo Use
`switchIfEmpty(Mono.error(new ProductoNoEncontradoException(id)))` en `buscarPorId()`:
si el id no existe, no tiene sentido dar un valor generico, porque el que pidio ese id
especifico necesita saber que no existe — por eso cambio el flujo a un error, que el
controlador traduce a HTTP 404 No son intercambiables porque uno resuelve "no hay
nada que mostrar, pero no es un error" y el otro resuelve "esto especifico que pediste
no existe, y eso si es un error para este caso"

**4.5** Por que `doOnNext` no sirve para transformar el elemento, si aparentemente
"recibe" el producto?

`doOnNext` recibe el elemento como parametro de su `Consumer`, pero un `Consumer` no
tiene valor de retorno (`void`) — solo puede ejecutar un efecto lateral, como mi
`LOG_PRODUCTO` que imprime en consola, pero no puede devolver un producto modificado
para que continue por la cadena El elemento que sigue fluyendo hacia abajo es
exactamente el mismo objeto que entro, sin cambios Para transformar un elemento
necesito `map`, que si recibe una `Function` con valor de retorno

---

## Fase 5 — Modulo de IA con LangChain4j

**5.1** Pega tu interfaz `AgroSmartAIService` completa

```java
@AiService
public interface AgroSmartAIService {

    @UserMessage("""
            Redacta una frase publicitaria de máximo 100 caracteres para vender \
            {{producto}} dirigido a {{audiencia}}.""")
    String generarPublicidad(@V("producto") String producto,
                              @V("audiencia") String audiencia);
}
```

**5.2** Que hace `@V("producto")` y que pasaria si lo quitaras dejando solo el
parametro?

`@V("producto")` le indica a LangChain4j que el valor de ese parametro del metodo debe
sustituir la variable `{{producto}}` dentro del prompt de `@UserMessage` Si lo
quitara, LangChain4j no sabria a que variable del prompt corresponde ese parametro
(sobre todo porque en tiempo de ejecucion los nombres de parametros no siempre estan
disponibles via reflexion sin compilar con informacion de depuracion), y probablemente
lanzaria un error indicando que no puede resolver `{{producto}}` en el prompt, o lo
dejaria sin reemplazar

**5.3** En que archivo y con que lineas configuraste el modelo? Por que **no** hizo
falta declarar un `@Bean`?

En `src/main/resources/application-prod.properties`:
```
langchain4j.open-ai.chat-model.api-key=demo
langchain4j.open-ai.chat-model.model-name=gpt-4o-mini
langchain4j.open-ai.chat-model.timeout=30s
```
No hizo falta declarar un `@Bean` porque el starter
`langchain4j-open-ai-spring-boot-starter` autoconfigura el `ChatModel` leyendo estas
propiedades directamente, de la misma forma que otros starters de Spring Boot
autoconfiguran su infraestructura a partir de `application.properties`

**5.4** Por que la llamada a la IA tambien necesita `boundedElastic`, si no es una
consulta a base de datos?

Porque igual que JPA, la llamada al modelo de LangChain4j es una peticion HTTP
bloqueante hacia el proveedor externo (OpenAI a traves de la clave demo): el hilo que
hace la llamada se queda esperando la respuesta de la red Bloquear no depende de si es
una base de datos o una API externa, sino de si el hilo se detiene a esperar en vez de
seguir procesando Por eso tambien la aislo con `subscribeOn(Schedulers.boundedElastic())`

**5.5** Si tu proveedor devolvio un error durante el examen, pega el mensaje real y la
respuesta que produjo tu `onErrorResume`

En mis pruebas manuales con `curl` la clave demo respondio correctamente casi siempre
(pegue ejemplos de frases generadas como "Descubre el sabor unico del cafe arabigo de
altura...") No me toco ver un fallo real en vivo, pero mi prueba unitaria
`PublicidadServiceTest.generarPublicidad_cuandoElProveedorFalla_debeEmitirMensajeDeRespaldo`
simula ese caso mockeando una excepcion `RuntimeException("429 Too Many Requests")`, y
confirma que mi `onErrorResume` produce el mensaje de respaldo
`"Publicidad no disponible en este momento (RuntimeException)"` sin propagar el error

---

## Fase 6 — API reactiva con WebFlux

**6.1** Pega la salida real de tus cuatro `curl`

```
> curl.exe http://localhost:8193/api/productos
[{"id":1,"nombre":"CAFE ARABIGO DE ALTURA","categoria":"Cafe","precioUsd":18.50,"correosNotificacion":["ventas@agrosmart.ec"]},{"id":2,"nombre":"CAFE ORGANICO SIERRA NORTE","categoria":"Cafe","precioUsd":21.90,"correosNotificacion":["ventas@agrosmart.ec","exportaciones@agrosmart.ec"]},{"id":3,"nombre":"CAFE TOSTADO PREMIUM","categoria":"Cafe","precioUsd":15.75,"correosNotificacion":["contacto@agrosmart.ec"]}]

> curl.exe http://localhost:8193/api/productos/1
{"id":1,"nombre":"Cafe arabigo de altura","categoria":"Cafe","precioUsd":18.50,"correosNotificacion":["ventas@agrosmart.ec"]}

> curl.exe -i http://localhost:8193/api/productos/9999
HTTP/1.1 404 Not Found
Content-Type: application/json
Content-Length: 127

{"timestamp":"2026-07-31T15:31:16.758Z","path":"/api/productos/9999","status":404,"error":"Not Found","requestId":"b6b89af7-5"}

> curl.exe "http://localhost:8193/api/agrosmart/publicidad?producto=Cafe%20arabigo%20de%20altura&audiencia=cafeterias%20de%20especialidad"
Descubre el sabor unico del cafe arabigo de altura: una experiencia sublime para tus clientes
```

**6.2** Como lograste que el id inexistente responda **404** y no 500?

Mi excepcion `ProductoNoEncontradoException` esta anotada con
`@ResponseStatus(HttpStatus.NOT_FOUND)` Cuando `buscarPorId` la lanza a traves de
`switchIfEmpty(Mono.error(...))`, Spring WebFlux detecta esa anotacion al propagar el
error hasta el controlador y traduce automaticamente la respuesta a HTTP 404, sin que
yo tenga que capturar el error manualmente en el controlador

**6.3** Que pasaria si tu controlador devolviera `List<Producto>` en lugar de
`Flux<Producto>`? Seguiria compilando? Seguiria siendo no bloqueante?

Seguiria compilando, porque `ProductoService` si me da un `Flux<Producto>` y yo podria
convertirlo a `List` con algo como `.collectList().block()` El problema es que
`block()` es exactamente la operacion bloqueante que WebFlux prohibe: obligaria al hilo
del event loop a esperar a que todo el flujo reactivo termine antes de poder responder,
anulando todo el proposito de usar WebFlux en primer lugar Dejaria de ser no
bloqueante, aunque el codigo siguiera compilando y funcionando "por fuera"

---

## Fase 7 — Pruebas unitarias

**7.1** Pega la salida real de tus pruebas (`./mvnw test` o `./gradlew test`)

```
[INFO] Running ec.edu.espe.agrosmart.domain.ProductoFiltersTest
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
[INFO] Running ec.edu.espe.agrosmart.domain.ProductoTest
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
[INFO] Running ec.edu.espe.agrosmart.service.ProductoServiceTest
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
[INFO] Running ec.edu.espe.agrosmart.service.PublicidadServiceTest
[INFO] Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
[INFO] Results:
[INFO] Tests run: 11, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

**7.2** Cuantos productos espera tu `expectNextCount(...)` y por que ese numero
concreto? Relacionalo con tu semilla

En `obtenerProductosComercializables_conTresValidosYDosInvalidos_debeEmitirSoloLosValidos`
espero `expectNextCount(3)` Este numero no depende de mi cedula ni de mi semilla
particular — el enunciado fija que todo el curso siembra 3 productos validos y 2
invalidos, asi que el flujo, tras filtrar con `IS_VALID`, siempre debe emitir
exactamente esos 3 productos validos, sin importar la categoria de cada quien

**7.3** Por que mockeaste `ProductoRepository` en lugar de dejar que la prueba consulte
PostgreSQL?

Para que mis pruebas sean rapidas, deterministas y no dependan de que PostgreSQL este
corriendo ni de la conexion de red Si la prueba consultara la base real, fallaria en
cualquier entorno donde la base no este disponible (por ejemplo, en un pipeline de CI),
y ademas dependeria del estado real de los datos en ese momento Con
`Mockito.mock(ProductoRepository.class)` controlo exactamente que devuelve
`findAll()` o `findById()` en cada prueba, aislando la logica de `ProductoService` de
la infraestructura

**7.4** Que demuestra `assertNotSame` que `assertEquals` **no** demuestra en tu prueba
de copia defensiva?

`assertEquals` solo verifica que el contenido de dos listas sea igual (mismos
elementos), pero dos listas distintas en memoria pueden tener contenido igual y
`assertEquals` pasaria igual aunque no sean copias defensivas reales `assertNotSame`
verifica que sean objetos distintos en memoria (identidad de referencia, no de
contenido) — eso es lo que realmente demuestra que mi getter devolvio una copia nueva
y no la misma lista interna que guarda el `Producto`

**7.5** Por que una prueba de un `Flux` que no llama a `verifyComplete()` (o a
`verify()`) no esta probando nada?

Un `Flux` o `Mono` es "lazy": la cadena de operadores que armo (`map`, `filter`,
`doOnNext`, etc) no se ejecuta hasta que algo se suscribe a ese flujo
`StepVerifier.create(flujo)` prepara las expectativas, pero es
`.verifyComplete()` (o `.verify()`) el que finalmente se suscribe y dispara la
ejecucion real del flujo, comprobando que las expectativas se cumplieron Si omito esa
llamada final, nunca se suscribe nadie, el flujo nunca se ejecuta, y la prueba pasa en
verde sin haber comprobado absolutamente nada

---

## Fase 8 — Integracion y cierre

**8.1** Pega tu `git log --oneline --graph --all`

```
*   58676e4 Merge pull request #9 from Anthony13S/feature/documentacion
|\
| * 611bf1f docs: agrego el repositorio en el readme
| * 4061e1a docs: documentacion decisiones, evidencias e instrucciones de ejecucion
| * 3690588 docs: agrege la entrada de decisiones de la fase 1 tambien de la fase 2
|/
*   1fc738d Merge pull request #8 from Anthony13S/feature/pruebas
|\
| * 1defe1f test: agrege pruebas del modelo, logica funcional, flujo reactivo y la ia
|/
*   b706bd4 Merge pull request #7 from Anthony13S/feature/api-reactiva
|\
| * f7cfe69 feat: endpoints reactivos y de publicidad
| * 1e1347c feat: expone endpoints reactivos y de publicidad
|/
*   959dabf Merge pull request #6 from Anthony13S/feature/ia-langchain4j
|\
| * ce03882 feat: integre langchain4j para la publicidad de productos
|/
*   417fd06 Merge pull request #5 from Anthony13S/feature/servicio-reactivo
|\
| * 3bcbbdc feat: implemente el servicio reactivo con boundedElastic y los operadores
|/
*   2d69eab Merge pull request #4 from Anthony13S/feature/modelo-inmutable
|\
| * 4c45ee5 feat: agrege modelo inmutable del producto y logica funcional del mismo
|/
*   4823089 Merge pull request #2 from Anthony13S/feature/persistencia-jpa
|\
| * 5cadc74 feat: se agrega entidad jpa de productos y siembra de los datos
* |   ca6b34a Merge pull request #1 from Anthony13S/feature/config-perfiles
|\ \
| |/
| * 1459b69 chore: configure perfil prod con postgresql y mi puerto propio
|/
* 303ef4e chore: actualizado pom.xml a java 21, agregado langchain4j y eliminados los archivos residuales
* a43fdd3 chore: inicializado proyecto agrosmart y registrada mi identidad en el examen
```

**8.2** Que fase te tomo mas tiempo del previsto y por que?

La Fase 1 me tomo mas tiempo del previsto porque planeaba usar Docker Compose Support
para levantar PostgreSQL automaticamente, pero Docker Desktop no pudo iniciar en mi
maquina por falta de soporte de virtualizacion detectado Tuve que cambiar de
estrategia a mitad de la fase: instalar PostgreSQL localmente, crear la base de datos
manualmente, y reconfigurar `application-prod.properties` con la conexion directa en
vez de depender de la autoconfiguracion de Docker Compose Support

**8.3** Si tuvieras 30 minutos mas, que mejorarias **primero** de tu entrega y por que
esa y no otra?

Agregaria manejo centralizado de excepciones con `@RestControllerAdvice` (uno de los
puntos de bonus del enunciado), porque actualmente mi unico manejo de error especifico
es `@ResponseStatus` en `ProductoNoEncontradoException`, y un `@RestControllerAdvice`
me permitiria estandarizar el formato de respuesta de error para cualquier excepcion
futura del proyecto, no solo esa La elegiria antes que las otras mejoras de bonus
porque tiene relacion directa con el flujo reactivo que ya construi, en vez de ser una
herramienta anadida aparte como Swagger

**8.4** Declara honestamente que herramientas consultaste durante el examen

Honestamente, consulte conceptos y documentacion de LangChain4j para la sintaxis de 
@AiService y @UserMessage a la inteligencia artificial y como podria arreglar el problema 
de la virtualizacion para poder usar la opcion A que era Docker Desktop, pero preferi
usar la opcion B que ya tenia instalada en mi computadora, tambien consulte que es lo que
tengo que hacer despues de commitear, ahi fue donde comprendi que despues de hacer el commit
debo de integrar eso en mi computadora, por ello los archuivos me salian de color rojo o amarrillo
despues de integrarlo en mi computdora salian de color verde y eso fui aprendiendo,  
el codigo lo escribi y lo entiendo como esta hecho, pero use el asistente como apoyo para entender 
el porque me salia de tal color y como realizar los commits e integrar todo en mi maquina


>
## Fase 1 (Realizado) — Configuración y perfiles

Active el perfil prod en application.properties porque necesito que la app arranque
siempre con mi configuracion de puerto (8193) y no con la de desarrollo por defecto
Al principio configure Docker Compose Support pero Docker Desktop no funciono en mi
maquina por falta de soporte de virtualizacion que no lo tengo activado en la bios y para no hacer 
mayor tiempo de duracion de la prueba cambie a la Opcion B
instale PostgreSQL localmente y declare spring.datasource.url, username y
password apuntando a mi base agrosmart_db creada con psql


## Fase 2 (Realizado) — Persistencia con JPA/Hibernate

Cree ProductoEntity mapeada a mi tabla tbl_productos_base_93 con las columnas exactas
que pide el enunciado: id_producto con IDENTITY, nombre_producto con length 120 y
unique, precio_usd como BigDecimal con precision 10 y escala 2. A diferencia de mi
modelo de dominio Producto, esta clase si tiene constructor vacio y setters porque
Hibernate los necesita para materializar los objetos al leer de la base de datos. Sembre los 5
productos (3 validos, 2 invalidos) con un CommandLineRunner que verifica
repository.count() == 0 para no duplicar en cada reinicio


## Fase 3 (Realizado) — Modelo inmutable y lógica funcional

Hice el Producto final y sin setters porque quiero que una vez creado, nadie pueda
cambiar su estado. La copia defensiva en el constructor evita que si alguien sigue
teniendo la lista de correos original la modifique y afecte mi objeto por dentro, la
del getter evita lo mismo pero al reves que quien reciba la lista desde afuera pueda
tocar mi estado interno. A_MAYUSCULAS no modifica el producto que recibe, construye uno
nuevo, por ultimo si mutara el original, estaria rompiendo la inmutabilidad que acabo de exigir


## Fase 4 (Realizado) — Servicio reactivo y aislamiento del bloqueo

Envolvi repository.findAll() en Mono.fromCallable().subscribeOn(boundedElastic) porque
JPA es una llamada bloqueante, si la ejecutara directo en el event loop de Netty,
bloquearia el hilo que atiende todas las peticiones concurrentes de la app. Use
defaultIfEmpty para el caso donde meciona que todos los productos son invalidos, porque el flujo sigue
completandose normalmente, solo cambia lo que se emite. En cambio use switchIfEmpty en
buscarPorId porque no encontrar un producto especifico por id es un error real, no un
caso vacio que se pueda rellenar con un valor generico

## Fase 5 (Realizado) — Módulo de IA con LangChain4j

Cree AgroSmartAIService como una interfaz con @AiService porque LangChain4j se encarga
de implementarla automaticamente en tiempo de ejecucion, leyendo el prompt de
@UserMessage y la configuracion que ya tenia en application-prod.properties. En
PublicidadService envolvi la llamada al modelo en boundedElastic porque es una llamada
HTTP bloqueante igual que JPA, y agregue onErrorResume porque la clave demo de
LangChain4j es compartida entre todo el curso y puede fallar, asi que
mi endpoint sigue respondiendo con un mensaje de respaldo en vez de caerse por completo 

## Fase 6 (Realizado) — API reactiva con WebFlux

Cree AgroSmartController con tres endpoints que solo conectan las rutas HTTP con los
servicios que ya tenia creados sin logica propia. Ninguno devuelve List ni un objeto bloqueante,
todo es Mono o Flux. Para el endpoint de publicidad tuve que agregar produces =
MediaType.TEXT_PLAIN_VALUE porque por defecto Spring devolvia el String envuelto en
JSON y el ejercicio pide en texto plano. Probe los 4 casos con curl: la lista de
productos uno por id, un id inexistente (confirme el 404) y la publicidad generada

## Fase 7 (Realizado) — Pruebas unitarias

Cubri ProductoTest con los getters y las dos copias defensivas (entrada y salida)
ProductoFiltersTest cubre el caso valido y los dos casos invalidos del Predicate
ProductoServiceTest mockea ProductoRepository con Mockito para no depender de
PostgreSQL y usa StepVerifier para probar los 3 casos: emision de los 3 validos,
emision del generico cuando todos son invalidos, y error cuando el id no existe
PublicidadServiceTest mockea AgroSmartAIService y cubre el camino correcto y el del fallo
del onErrorResume. Las 11 pruebas pasan en verde sin tocar la base de datos ni internet